// Drizzle table definitions will live here.
// Keep identifiers (schema, table, columns) snake_case to exactly match the live DB.
// Example (to be added once drizzle packages are available):
// import { pgSchema, text, varchar, jsonb, timestamp } from "drizzle-orm/pg-core";
// export const Auth = pgSchema("auth");
// export const organization = Auth.table("organization", { ... });

export {};

